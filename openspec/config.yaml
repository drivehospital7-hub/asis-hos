# openspec/config.yaml — SDD Configuration for asis-hos (control_system_dev)
# Auto-generated by sdd-init on 2026-05-28 (updated)
schema: spec-driven

context: |
  Project: asis-hos (control_system_dev) — Medical Billing Control System for EPS MALLAMAS (indigenous healthcare provider).
  Stack: Flask 3.1.3 + Polars 1.39 (Excel reading) + openpyxl 3.1 (Excel writing) + waitress 3.0 (production server) + SQLAlchemy + PostgreSQL (psycopg3).
  Architecture: Flask routes → services (odontologia/urgencias/equipos_basicos/transversales) → utils. Routes are thin delegators; services are SRP with one detector per file; each domain has a detect_all.py orchestrator.
  Testing: pytest (46 test files) with Flask test client (app_client fixture), unit + integration tests, pytest-cov for coverage.
  Conventions: AGENTS.md defines code rules; CONVENTIONS.md defines business rules.
  Frontend: Flask templates + React (Vite + Tailwind CSS + shadcn/ui + Lucide), built at frontend/.
  SDD state: 10 active changes, 17 archived changes in openspec/changes/.

testing:
  strict_tdd: true
  test_runner:
    command: pytest
    framework: pytest 7+
    method: python -m pytest (no venv detected; run via installed packages)
    options: -v (default addopts)
  test_layers:
    unit: true
    integration: true
    e2e: false
  coverage:
    available: true
    tool: pytest-cov 7.1.0 + coverage 7.14.0
    command: python -m pytest --cov
    report_file: .coverage
  quality_tools:
    linter: none
    type_checker: none
    formatter: none

rules:
  proposal:
    - Include intent, scope, and approach for each change.
  specs:
    - Use Given/When/Then for scenarios.
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY).
  design:
    - Include sequence diagrams for complex flows.
    - Document architecture decisions with rationale.
  tasks:
    - Group by phase, use hierarchical numbering.
    - Keep tasks completable in one session.
  apply:
    - Follow existing code patterns (AGENTS.md).
    - Run pytest before committing on any change.
    tdd: true
    test_command: python -m pytest -v
  verify:
    test_command: python -m pytest -v
    build_command: python run_dev.py
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas.
